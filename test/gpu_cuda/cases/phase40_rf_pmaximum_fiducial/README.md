# phase40_rf_pmaximum_fiducial

Focused Action 6 regression for serial/local RF-only `FIDUCIAL="pmaximum"`
phase setup.  The lattice exercises zero-length thin RFCA, nonzero-length
matrix-method RFCA, kick-method RFCA, RF-only matrix-method RFCW, and RF-only
kick-method RFCW, with deterministic offsets, end focusing, and `CHANGE_P0`
inside the guarded subsets.
