# phase37_rfcw_multikick

Focused RFCW regression for the CUDA kick-method loop when `N_KICKS>1`.
The lattice combines a two-kick `WAKES_AT_END=0` section and a three-kick
`WAKES_AT_END=1` section, both with longitudinal/transverse wakes and
fixed-bin filtered LSC.
