# phase41_rfcw_wake_pmaximum_fiducial

Focused Action 6 regression for serial/local wake-bearing `RFCW`
`FIDUCIAL="pmaximum"` phase setup.  The lattice keeps the existing guarded
matrix-method and multi-kick collective shapes, with longitudinal/transverse
wakes, fixed-bin filtered LSC kicks, deterministic offsets, focusing,
`CHANGE_P0`, and `WAKES_AT_END=0|1`.
