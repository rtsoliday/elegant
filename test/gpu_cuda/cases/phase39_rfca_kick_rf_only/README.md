# phase39_rfca_kick_rf_only

Focused RFCA regression for the CUDA RF-only kick-method path when
`N_KICKS>0` and no wake or LSC arguments are active.  The lattice exercises
`LIGHT` and serial/local `TMEAN` fiducialization, deterministic offsets,
entrance/exit focusing, and `CHANGE_P0=1`.
